{
  "name": "alerta-espana",
  "version": "0.1.0",
  "private": true,
  "description": "Mapa nacional de riesgos en tiempo real (lluvia, DANA, riadas) con alertas por proximidad.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "dev": "echo \"Abre DOS terminales:\\n  1) npm --prefix server run dev\\n  2) npm --prefix web run dev   -> http://localhost:5173\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
